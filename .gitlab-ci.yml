image: node:14.13.1
pages:
    stage: deploy
    script:
      - cd website
      - yarn install
      - yarn run build
      - mkdir .public
      - cp -r build/* .public
      - mv .public ../public
    artifacts:
        paths:
          - public
    #only:
    #  - master
